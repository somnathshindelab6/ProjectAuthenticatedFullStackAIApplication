# Copy to .env and fill in values for production
DATABASE_URL=sqlite:///dev.db
FLASK_SECRET_KEY=replace-with-a-random-secret
JWT_SECRET_KEY=replace-with-jwt-secret
OPENAI_API_KEY=
VECTOR_DB_URL=
PORT=5000

# For production, set DATABASE_URL to a Postgres instance, and consider using pgvector for embeddings.