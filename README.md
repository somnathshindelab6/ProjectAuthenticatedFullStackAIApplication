# Project: Smart Task Management (Authenticated Full-Stack AI Application)

This workspace contains a minimal Flask backend scaffold for the capstone project. The backend lives in the `backend/` folder and includes models, authentication endpoints, and a stubbed AI/RAG endpoint.

See `backend/README.md` for setup and run instructions.

