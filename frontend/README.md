# Frontend (React)

Simple React frontend scaffold for the Smart Task Management app.

Setup

```bash
cd frontend
npm install
npm start
```

Notes
- The frontend expects the backend API at `http://localhost:5000` by default. Update `frontend/src/api.js` if needed.
